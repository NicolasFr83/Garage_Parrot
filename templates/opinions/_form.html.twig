<section>
{{ form_start(form, {'attr': {'class': 'opinion-new'}}) }}
    {{ form_widget(form) }}
    <button class="btn buton">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</section>
